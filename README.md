# ML Chess Arena

A responsive, good-looking browser chess game where you play White against a black AI opponent.

This version is fully self-contained (no external CDN dependency), so pieces and gameplay load reliably in local/offline and restricted-network environments.

## Run

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
